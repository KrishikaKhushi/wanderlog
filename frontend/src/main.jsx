import React from 'react';
import { createRoot } from 'react-dom/client';
import CoverPage from './App.jsx'; // or rename file to CoverPage.jsx

createRoot(document.getElementById('root')).render(<CoverPage />);
